import { AddtaxMemoPipe } from './addtax-memo.pipe';

describe('AddtaxMemoPipe', () => {
  it('create an instance', () => {
    const pipe = new AddtaxMemoPipe();
    expect(pipe).toBeTruthy();
  });
});
